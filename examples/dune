(executables
 (names sdf_mesh)
 (libraries OCADml OManifold))

(rule
 (alias examples)
 (deps
  (:runner "helpers/runner.exe")
  (:models
   (glob_files "*.exe")))
 (targets
  (dir "stls"))
 (mode promote)
 (action
  (chdir
   "stls"
   (run %{runner} %{models}))))
